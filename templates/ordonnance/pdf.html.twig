<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Prescription #{{ ordonnance.id }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #2c3e50;
            padding-bottom: 20px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        .doctor-info {
            font-size: 16px;
            color: #666;
        }
        .prescription-details {
            margin: 30px 0;
        }
        .patient-info {
            margin-bottom: 30px;
            padding: 15px;
            background-color: #f8f9fa;
            border-radius: 5px;
        }
        .prescription-content {
            margin: 20px 0;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            min-height: 300px;
        }
        .footer {
            margin-top: 50px;
            text-align: right;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
        .signature {
            margin-top: 20px;
            font-style: italic;
        }
        .watermark {
            position: fixed;
            bottom: 0;
            right: 0;
            opacity: 0.1;
            font-size: 100px;
            transform: rotate(-45deg);
            color: #2c3e50;
            z-index: -1;
        }
    </style>
</head>
<body>
    <div class="watermark">TBIBI</div>
    
    <div class="header">
        <div class="logo">TBIBI Medical Center</div>
        <div class="doctor-info">
            Dr. {{ ordonnance.consultation.medecin.nom }} {{ ordonnance.consultation.medecin.prenom }}<br>
            License No: {{ ordonnance.consultation.medecin.id }}<br>
            Email: {{ ordonnance.consultation.medecin.email }}
        </div>
    </div>

    <div class="prescription-details">
        <div class="patient-info">
            <strong>Patient Information:</strong><br>
            Name: {{ ordonnance.consultation.patient.nom }} {{ ordonnance.consultation.patient.prenom }}<br>
            ID: {{ ordonnance.consultation.patient.id }}<br>
            Date: {{ ordonnance.consultation.dateC|date('F d, Y') }}<br>
            Consultation Type: {{ ordonnance.consultation.type.value|title }}
        </div>

        <div class="prescription-content">
            <h3>Prescription</h3>
            {{ ordonnance.description|nl2br }}
        </div>
    </div>

    <div class="footer">
        <div>Date: {{ "now"|date('F d, Y') }}</div>
        <div class="signature">
            Digital Signature:<br>
            {{ ordonnance.signature }}
        </div>
    </div>
</body>
</html>
