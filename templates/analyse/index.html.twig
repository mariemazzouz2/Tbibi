{% extends 'base.html.twig' %}

{% block title %}Analyse{% endblock %}

{% block body %}
    <style>
        /* Conteneur principal */
        .analyse-container {
            max-width: 1000px;
            margin: 40px auto;
            padding: 30px;
            background: white;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        /* Titre */
        .analyse-header {
            text-align: center;
            font-size: 26px;
            font-weight: bold;
            color: #1A76D1;
            margin-bottom: 30px;
        }

        /* Table */
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        .table th {
            background: #1A76D1;
            color: white;
            padding: 12px;
            text-align: left;
            border-radius: 5px;
        }

        .table td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
            text-align: center;
        }

        /* Espacement des boutons */
        .btn-container {
            margin-top: 20px;
            text-align: center;
        }

        .btn {
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 15px;
            margin: 5px;
            display: inline-block;
        }

        .btn-primary {
            background-color: blue;
            color: white;
        }

        .btn-primary:hover {
            background-color: darkblue;
        }

        .btn-warning {
            background-color: #ffc107;
            color: black;
        }

        .btn-warning:hover {
            background-color: #d39e00;
        }

        .btn-success {
            background-color: #28a745;
            color: white;
        }

        .btn-success:hover {
            background-color: #1e7e34;
        }
<<<<<<< HEAD

        /* Style pour la pagination */
        .navigation {
            margin-top: 20px;
            text-align: center;
        }

        .pagination {
            display: inline-flex;
            list-style: none;
            padding: 0;
        }

        .pagination li {
            margin: 0 5px;
        }

        .pagination a, .pagination span {
            display: inline-block;
            padding: 8px 12px;
            border: 1px solid #1A76D1;
            border-radius: 5px;
            text-decoration: none;
            color: #1A76D1;
            background-color: #fff;
            transition: all 0.3s ease;
        }

        .pagination .active span {
            background-color: #1A76D1;
            color: white;
            border-color: #1A76D1;
        }

        .pagination a:hover {
            background-color: #e6f0fa;
        }

        .pagination .disabled span {
            color: #aaa;
            border-color: #ddd;
            background-color: #f5f5f5;
            cursor: not-allowed;
        }

        /* Style pour la barre de recherche */
        .search-container {
            margin-bottom: 20px;
            text-align: center;
        }

        .search-container input {
            padding: 10px;
            width: 50%;
            border: 1px solid #1A76D1;
            border-radius: 5px;
            font-size: 16px;
        }
=======
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
    </style>

    <div class="analyse-container">
        <div class="analyse-header">📊 Liste des Analyses</div>

<<<<<<< HEAD
        <!-- Barre de recherche -->
        <div class="search-container">
            <input type="text" id="searchInput" placeholder="Rechercher par type, date ou diagnostic..." data-dossier-id="{{ dossierId }}">
        </div>

=======
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Type</th>
                    <th>Date</th>
                    <th>Données</th>
                    <th>Diagnostic</th>
                    <th>Actions</th>
                </tr>
            </thead>
<<<<<<< HEAD
            <tbody id="analyseTableBody">
=======
            <tbody>
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
            {% for analyse in analyses %}
                <tr>
                    <td>{{ analyse.id }}</td>
                    <td>{{ analyse.type }}</td>
                    <td>{{ analyse.dateanalyse ? analyse.dateanalyse|date('d/m/Y') : 'Non spécifiée' }}</td>
                    <td>
                        {% if analyse.donneesAnalyse %}
<<<<<<< HEAD
                            <a href="{{ asset('uploads/' ~ analyse.donneesAnalyse) }}" target="_blank" class="btn btn-primary">📄 Aperçu</a>
                        {% else %}
                            Aucun fichier
                        {% endif %}
                    </td>
                    <td>{{ analyse.diagnostic }}</td>
=======
                            <a href="{{ asset('uploads/' ~ analyse.donneesAnalyse) }}" target="_blank" class="btn btn-primary">📄apperçu</a>
                        {% else %}
                            Aucun fichier
                        {% endif %}
                    </td>                    <td>{{ analyse.diagnostic }}</td>
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
                    <td>
                        <a href="{{ path('app_analyse_show', {'id': analyse.id}) }}" class="btn btn-primary">👀 Afficher</a>
                        <a href="{{ path('app_analyse_edit', {'id': analyse.id}) }}" class="btn btn-warning">✏️ Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

<<<<<<< HEAD
        {# Pagination personnalisée #}
        <div class="navigation">
            {{ knp_pagination_render(analyses, null, {}, {
                'prev_label': '« Précédent',
                'next_label': 'Suivant »',
                'align': 'center'
            }) }}
        </div>

=======
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
        <div class="btn-container">
            <a href="{{ path('app_analyse_new') }}" class="btn btn-success">➕ Nouvelle Analyse</a>
        </div>
    </div>
<<<<<<< HEAD

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const searchInput = document.getElementById('searchInput');
            const tableBody = document.getElementById('analyseTableBody');
            const dossierId = searchInput.getAttribute('data-dossier-id');
            let debounceTimeout;

            searchInput.addEventListener('input', function () {
                clearTimeout(debounceTimeout);
                debounceTimeout = setTimeout(() => {
                    const searchTerm = this.value.trim();

                    fetch(`/analyse/search/${dossierId}?q=${encodeURIComponent(searchTerm)}`, {
                        method: 'GET',
                        headers: {
                            'Accept': 'application/json',
                        },
                    })
                    .then(response => response.json())
                    .then(data => {
                        tableBody.innerHTML = ''; // Vider le tableau

                        if (data.length === 0) {
                            tableBody.innerHTML = '<tr><td colspan="6">Aucun enregistrement trouvé</td></tr>';
                            return;
                        }

                        data.forEach(analyse => {
                            const row = `
                                <tr>
                                    <td>${analyse.id}</td>
                                    <td>${analyse.type}</td>
                                    <td>${analyse.dateanalyse}</td>
                                    <td>
                                        ${analyse.donneesAnalyse 
                                            ? `<a href="/uploads/${analyse.donneesAnalyse}" target="_blank" class="btn btn-primary">📄 Aperçu</a>` 
                                            : 'Aucun fichier'}
                                    </td>
                                    <td>${analyse.diagnostic}</td>
                                    <td>
                                        <a href="/analyse/show/${analyse.id}" class="btn btn-primary">👀 Afficher</a>
                                        <a href="/analyse/${analyse.id}/edit" class="btn btn-warning">✏️ Modifier</a>
                                    </td>
                                </tr>
                            `;
                            tableBody.innerHTML += row;
                        });
                    })
                    .catch(error => {
                        console.error('Erreur lors de la recherche:', error);
                        tableBody.innerHTML = '<tr><td colspan="6">Erreur lors de la recherche</td></tr>';
                    });
                }, 300); // Délai de 300ms pour le debouncing
            });
        });
    </script>
{% endblock %}
=======
{% endblock %}
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
