{% extends 'base.html.twig' %}

{% block title %} Analyse #{{ analyse.id }} {% endblock %}

{% block body %}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Analyse #{{ analyse.id }}</title>

    <!-- Bootstrap 5 -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <!-- Custom Styles -->
    <style>
        .rounded-3 { border-radius: 12px; }
        .rounded-4 { border-radius: 16px; }
        .fw-bold { font-weight: bold; }
        .fw-semibold { font-weight: 600; }
        .btn-custom { border-radius: 50px; padding: 10px 20px; }
    </style>
</head>
<body>

<div class="container mt-5">
    <div class="card shadow-lg p-4 border-0 rounded-4">
        <!-- Titre -->
        <h2 class="text-center text-primary fw-bold mb-4">
            <i class="fas fa-chart-bar"></i> Analyse #{{ analyse.id }}
        </h2>

        <div class="row">
            <!-- Informations de l'Analyse -->
            <div class="col-md-6">
                <h4 class="text-secondary fw-semibold"><i class="fas fa-stethoscope"></i> Informations</h4>
                <div class="p-3 border rounded-3 bg-light">
                    <p><strong>ID :</strong> {{ analyse.id }}</p>
                    <p><strong>Type :</strong> {{ analyse.type|default('Non spécifié') }}</p>
                    <p><strong>Date :</strong> {{ analyse.dateanalyse ? analyse.dateanalyse|date('d/m/Y') : 'Non définie' }}</p>
                </div>
            </div>

            <!-- Résultats -->
            <div class="col-md-6">
                <h4 class="text-secondary fw-semibold"><i class="fas fa-clipboard-list"></i> Résultats</h4>
                <div class="p-3 border rounded-3 bg-light">
<<<<<<< HEAD
                    <p><strong>Données Analyse :</strong> {{ analyse.donneesAnalyse|default('Aucune donnée') }}</p>
=======
                    <p><strong>Données Analyse :</strong> {{ analyse.donnees_analyse|default('Aucune donnée') }}</p>
>>>>>>> 923de995026e55cf0fef03fa57b66e233e64a609
                    <p><strong>Diagnostic :</strong> {{ analyse.diagnostic|default('Non disponible') }}</p>
                </div>
            </div>
        </div>

        <!-- Boutons d'actions -->
        <div class="mt-4 text-center">
            <a href="{{ path('app_analyse_index', {'dossierId': analyse.dossier.id })  }}" class="btn btn-primary">
                 <i class="fas fa-arrow-left"></i> Retour À La Liste
            </a>
            <a href="{{ path('app_analyse_edit', {id: analyse.id}) }}" class="btn btn-warning text-white btn-custom me-2">
                <i class="fas fa-edit"></i> Modifier
            </a>
            <a href="{{ path('app_analyse_delete', {id: analyse.id}) }}" class="btn btn-danger btn-custom"
               onclick="return confirm('Voulez-vous vraiment supprimer cette analyse ?')">
                <i class="fas fa-trash"></i> Supprimer
            </a>
        </div>
    </div>
</div>

<!-- Scripts Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
{% endblock %}
