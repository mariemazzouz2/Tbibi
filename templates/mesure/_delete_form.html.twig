<form method="post" action="{{ path('app_mesure_delete', {'id': mesure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mesure.id) }}">
    <button class="btn">Delete</button>
</form>
