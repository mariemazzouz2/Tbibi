<form method="post" action="{{ path('app_prediction_delete', {'id': prediction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prediction.id) }}">
    <button class="btn">Delete</button>
</form>
